//Saber si un valor es verdadero o falso 

//Valores que nos devuelve False 
Boolean(0);
Boolean(nul);
Boolean(NaN);
Boolean(undefined);
Boolean(false);
Boolean("");



//Valores que nos devuelve verdadero
Boolean(1); //número diferente de cero (0)
Boolean("a"); //caracter o espacio en blanco en un string
Boolean(true);
Boolean([]); //array vacio
Boolean({}); //objeto vacio
Boolean(function () {}); //Cualquier función